// ── Index: thêm nhân vật mới chỉ cần import và đưa vào mảng ──
import { Character } from '../types';
import denia from './characters/denia';
import aemeath from './characters/aemeath';

export const charactersData: Character[] = [
  denia,
  aemeath,
];
